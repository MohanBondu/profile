const logos = require('@iconify-json/logos/icons.json');
const mdi = require('@iconify-json/mdi/icons.json');

console.log('=== LOGOS (java) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('java')).slice(0, 20));

console.log('\n=== MDI (folder) ===');
console.log(Object.keys(mdi.icons).filter(k => k.includes('folder')).slice(0, 20));

console.log('\n=== LOGOS (spring) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('spring')).slice(0, 20));

console.log('\n=== LOGOS (angular) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('angular')).slice(0, 20));

console.log('\n=== LOGOS (django) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('django')).slice(0, 20));

console.log('\n=== LOGOS (fastapi) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('fastapi')).slice(0, 20));

console.log('\n=== LOGOS (react) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('react')).slice(0, 20));

console.log('\n=== LOGOS (docker) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('docker')).slice(0, 20));

console.log('\n=== LOGOS (kubernetes) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('kubernetes')).slice(0, 20));

console.log('\n=== LOGOS (redis) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('redis')).slice(0, 20));

console.log('\n=== LOGOS (docker) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('docker')).slice(0, 20));

console.log('\n=== LOGOS (redis) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('redis')).slice(0, 20));

console.log('\n=== LOGOS (prometheus) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('prometheus')).slice(0, 20));

console.log('\n=== LOGOS (github) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('github')).slice(0, 20));

console.log('\n=== LOGOS (linkedin) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('linkedin')).slice(0, 20));

console.log('\n=== LOGOS (twitter) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('twitter')).slice(0, 20));

console.log('\n=== LOGOS (docker) ===');
console.log(Object.keys(logos.icons).filter(k => k.includes('docker')).slice(0, 20));